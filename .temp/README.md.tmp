# <%= projectName %>

## Project setup
```
<%= pkg %> install
```

### Compiles and hot-reloads for development
```
<%= pkg %><%= pkg !== 'yarn' ? 'run' : '' %> serve
```

### Compiles and minifies for production
```
<%= pkg %><%= pkg !== 'yarn' ? 'run' : '' %> build
```

### Lints and fixes files
```
<%= pkg %><%= pkg !== 'yarn' ? 'run' : '' %> lint
```