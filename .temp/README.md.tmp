# <%= projectName %>

## Project setup
```
<%= packageManager %> install
```

### Compiles and hot-reloads for development
```
<%= packageManager %><%= packageManager !== 'yarn' ? 'run' : '' %> serve
```

### Compiles and minifies for production
```
<%= packageManager %><%= packageManager !== 'yarn' ? 'run' : '' %> build
```

### Lints and fixes files
```
<%= packageManager %><%= packageManager !== 'yarn' ? 'run' : '' %> lint
```